# Recipe: Mini Memory‑RAG

- Ingest notes as `memory().record(kind="note", data=...)`
- Simple retrieval via `memory().recent()/query()` into `llm().chat()`

*(Implementation stub — fill with your runtime specifics.)*
