# Config Keys

- `AETH_PORT` — server port
- `LOG_LEVEL` — logging level (e.g., INFO)
- `MODEL_NAME` — default LLM
- `SLACK_*` — Slack integration (bot token, signing secret)

*(Expand with your actual config schema.)*
