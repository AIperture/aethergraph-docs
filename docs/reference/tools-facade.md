# Tools Facade

### register(func, *, name=None, inputs=None, outputs=None) → str
Registers a tool and returns its name/id.

### call(name, args: dict) → dict
Invokes a tool by name with validated args.
